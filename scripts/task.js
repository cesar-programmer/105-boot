class Task {
  constructor (title, description, startDate, color, status) {
    this.title = title
    this.description = description
    this.color = color
    this.startDate = startDate
    this.status = status
    this.budget = 0

    this.name = 'Cesar'
  }
}
